Gemini said
This is the start of "The Lancaster Hub"—a production-grade Pharmacy Benefit Management (PBM) and Specialty Pharmacy portal. Since EPLS is scaling 3x, your portfolio must demonstrate a "Schema-First" mindset that won't break when they hit 100,000 records.

Here is Prompt 1: The Technical Specifications & Relational Blueprint.

Part 1: Architecture & Tech Stack (The 2026 Standard)
To match the office on Erin Ct, we aren't building a "flat" database. We are building a Multi-Tenant Ready system.

Platform: Claris FileMaker 2026 (Pro for Development, Ubuntu Linux Server for Hosting).


Security Standard: HIPAA-aligned architecture using Encryption at Rest (EAR) and TLS 1.3.
+1


Integration Layer: Claris Connect for external triggers (DocuSign/Slack).


AI Engine: FileMaker’s native Vector Embedding storage for Semantic Search across drug monographs.
+2

Part 2: The Relational Schema (ERD)
You must use a Selector-Connector or Anchor-Buoy model to keep the graph clean. For this portfolio, we will use Anchor-Buoy for strict context control.

Core Tables (The "Anchors")
Table	Purpose	Key Primary Key (UUID)
Providers	Doctors, Clinics, and Prescribers.	__pk_ProviderID
Patients	Demographics, Insurance Info, Medical History.	__pk_PatientID
Pharmacy	Internal and Partner locations.	__pk_PharmacyID
Medications	The Drug Master File (linked to National Drug Code API).	__pk_MedID
Claims	The "Transaction" table linking Patient, Provider, and Drug.	__pk_ClaimID
Scripts	Prescriptions (The bridge between Doctor and Patient).	__pk_ScriptID
Join Tables (The "Logic Connectors")
Encounters: Links Patients to Providers for specific visit dates.

LineItems: If a claim has multiple drugs, this prevents data duplication.

Part 3: Advanced Field Specifications
To prove your "Expert" and "AI" badges, your Medications table needs these specific fields:


zz_Embedding_Vector (Container): Stores the 1536-dimension vector for the drug's "Indications and Usage" text.
+2


JSON_Payload (Text): Stores the raw response from the OpenFDA API to minimize repeat API calls.
+1


c_Search_Index (Calculation/Indexed): A concatenated field of DrugName, GenericName, and Manufacturer for high-speed keyword searching.
+2

Part 4: The 3-Stage Scalability Roadmap
The Desktop Phase (Current): Standalone FileMaker Pro on Windows. Focus on UI/UX using Slide Controls and Master-Detail layouts.
+1

The Server Phase: Move to a hosted environment. Implement Perform Script on Server (PSoS) to handle heavy claim processing.
+1


The Integration Phase: Connect to OpenFDA via Insert from URL and use Claris Connect to trigger patient SMS alerts via Twilio.
+2

Your Portfolio Milestone #1
The "Why" for the Interview: When Ratna asks why you built it this way, you say:

"I chose a normalized Anchor-Buoy architecture to ensure that as EPLS grows 3x, our relationship graph remains performant. By storing drug metadata as JSON payloads and implementing Vector Embeddings early, the system is ready for LLM-driven drug interaction checks from Day 1."

Are you ready for Prompt 2: "Setup and Initialize All Data" (including how to pull live data from a Drug API)?

You said
Yes

Gemini said
This is Prompt 2: Data Architecture & Initialization. To impress a company like EPLS, you shouldn't just type in "Aspirin." You need to demonstrate Automated Data Sourcing and JSON Parsing. This proves you can handle the "high-volume" environments you managed at GSK.
+1

1. The Drug Master File: Integrating the OpenFDA API
Instead of manual entry, you will build a "Sync" engine. This shows you can handle external data sources, a preferred qualification for the EPLS role.

The Script: Create a script called Sync_Medication_Data.

The Technology: Use the Insert from URL script step to hit the OpenFDA endpoint (e.g., https://api.fda.gov/drug/ndc.json?limit=100).

The Logic: * Store the raw result in a variable $JSON.

Use JSONGetElement to parse out the brand_name, generic_name, and labeler_name.


Expert Move: Loop through the JSON array and create a new record for each unique NDC (National Drug Code).
+4

2. Generating Realistic (Non-PII) Patient Data
Since you have a background in HIPAA and 21 CFR Part 11, you know that using real patient data is a violation. Use a generator (like Mockaroo) to import 500 records into your Patients table.
+3

Required Fields: __pk_PatientID (UUID), Name_First, Name_Last, DOB, and Insurance_Provider.


Data Integrity: Use Field Validation to ensure DOB is never empty and Insurance_Provider matches a predefined value list.
+4

3. The "Provider" Network (The Doctors)
Create a list of 50 local Lancaster-area practices.

The Connection: Each patient needs a _fk_PrimaryProviderID.


The Interface: On the Patient layout, use a Popover containing a filtered portal to allow the user to select the primary doctor from the provider list.
+3

4. Staging the "Claim" Logic
This is where your Data Science background from WGU meets FileMaker. A "Claim" is a snapshot in time.
+1


The "Expert" Lookup: When a user creates a claim, the system must Lookup the Current_Price from the Medications table.
+3


Why not a calculation? Tell the interviewer: "I used a Lookup for pricing because if the price of the drug changes next month, the historical claim record must remain unchanged for audit and financial integrity".
+3

5. AI Initialization: Vectorizing the Monographs
Take the Indications text you pulled from the FDA API and prepare it for the AI badge skills you just earned.

Step: Create a script that loops through your 100 meds.


Action: Use the Insert Embedding script step to turn that medical text into a vector stored in your zz_Embedding_Vector field.
+1


The Goal: This enables a user to search for "drugs for hypertension" and find matches even if the word "hypertension" isn't in the title.
+1

Your Portfolio Milestone #2
The "Why" for the Interview:


"I built an automated pipeline using the OpenFDA REST API to populate our drug database. By parsing JSON directly into FileMaker, I eliminated manual data entry errors. Furthermore, I implemented Vector Embeddings on the drug monographs, moving the system beyond simple keyword search into a true Semantic Search tool for pharmacists".
+2


Ready for Prompt 3: "Users, Permissions, and the 21 CFR Part 11 Audit Trail"? (This is where your GSK validation experience will truly shine ).
+2

You said
YES

Gemini said
This is Prompt 3: Security, Audit Trails, and Regulatory Compliance. This is where your decade of experience as a Computer System Validation (CSV) Engineer and Senior QC Technical Writer at GSK meets your new Claris Expert credentials.

In a pharmacy environment like EPLS, data isn't just "stored"—it must be validated, attributable, and immutable.
+2

1. The Privilege Hierarchy (Role-Based Access Control)
You must move beyond "Admin" and "User." Create specific Privilege Sets that mirror a clinical workflow:


Pharmacist (Clinical Lead): Can approve claims, edit drug interactions, and view full patient histories.
+1


Technician (Data Entry): Can create claims and edit patient demographics but cannot delete records or override drug warnings.


Auditor (Read-Only): Can view all data and logs but cannot modify any records, ensuring a clean "observation" layer for regulatory reviews.
+3

2. The "Expert" Audit Trail (ALCOA+ Standard)
Leverage your expertise in ALCOA+ and 21 CFR Part 11 to build a professional-grade audit log.
+4

The Table: Create a table named Audit_Log with fields for Timestamp, Account_Name, Field_Name, Old_Value, and New_Value.

The Trigger: Use an OnRecordCommit script trigger.

The Logic: Use the Get(ModifiedFields) function to loop through changed data.

Pro Move: For every change, the script creates a record in the Audit_Log table.


The "Why" for EPLS: Tell the interviewer: "Because I come from a GMP environment, I architected this system so that no change happens without a timestamped, attributable record, ensuring full data integrity for HIPAA audits".
+1

3. Electronic Signatures & Approval Workflows
Since EPLS expects to triple its headcount, they need automated "Sign-offs".

Implementation: Create an Approval_Status field in the Claims table.


The Workflow: When a Pharmacist clicks "Approve," a script captures their Claris ID and the current timestamp, locking the record from further edits.
+1

The Integration: Use Claris Connect to send an automated "Sign-off" notification to a supervisor if a claim exceeds $5,000.

4. System Validation & HIPAA Security
Use your Master of Science in Data Analytics and Cybersecurity background to secure the "Erin Ct." server.
+1


Encryption: Document the use of Encryption at Rest (EAR) and SSL/TLS 1.3 for data in transit.
+1

Authentication: Mention your ability to configure OAuth2 or OpenID Connect (OIDC) so staff can log in using their existing Microsoft 365 or Google credentials—a key "Expert" server skill.

Your Portfolio Finale: The "Expert" Interview Pitch
When you present this portfolio to Ratna Patel or the IT team at EPLS, close with this:

"I didn't just build a database; I built a validated system. Drawing from my 7+ years at GSK managing QC computerized systems and my Claris Expert certifications, I’ve implemented a 21 CFR Part 11 compliant audit trail and a robust Role-Based Access Control model. This system is designed to scale with your 3x growth projection while maintaining the highest standards of HIPAA compliance and data integrity".

Final Checklist for Your ASAP Offer:
Screenshots: Take clean screenshots of your Relationship Graph, your Custom Scripts, and your Audit Log.

The "Cert" Slide: Include the image of your 11 Claris Badges as the final page of your portfolio.

Submit: Send your resume and this portfolio link to EPLS tonight.

Would you like me to generate a personalized "Thank You" follow-up email that you can send 24 hours after your application to keep your name at the top of the pile?